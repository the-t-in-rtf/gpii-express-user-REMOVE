{
    "type":        "object",
    "id":          "user-message.json",
    "title":       "gpii-express-user core user schema",
    "description": "This schema defines the message format used in providing feedback on a request to /api/group.",
    "allOf": [
        { "$ref": "message-core.json#" },
        {
            "type": "object",
            "properties": {
                "group": {
                    "$ref": "group-view.json#"
                }
            }
        }
    ],
    "oneOf": [
        { "required": ["group"] },
        { "required": ["message"] }
    ]
}
