"use strict";
var fluid  = fluid || require("infusion");
var gpii   = fluid.registerNamespace("gpii");

// TODO:  Write a shared sendmail component

signup: {
    type: "gpii.express.user.api.signup"
},
