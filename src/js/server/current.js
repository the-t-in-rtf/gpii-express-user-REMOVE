"use strict";
var fluid  = fluid || require("infusion");
var gpii   = fluid.registerNamespace("gpii");

fluid.registerNamespace("gpii.express.user.api.current");

// TODO:  Back this with req.session

fluid.defaults("gpii.express.user.api.current", {
    // TODO: Start with request aware router
});
